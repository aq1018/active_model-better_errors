# encoding: utf-8

# ActiveModel uses `autoload` directive,
# we want to pre-autoload the parts that are needed for tests

# rubocop:disable Void
ActiveModel
# rubocop:enable Void

ActiveModel::Errors
